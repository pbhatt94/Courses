package design_patterns.singleton;

public class SingletonRunner {

	public static void main(String[] args) {
		//		DbConnectionEager connection = DbConnectionEager.getInstance();
		//		DbConnectionLazy connection = DbConnectionLazy.getInstance();
		//		DbConnectionSynchronized connection = DbConnectionSynchronized.getInstance();
		DbConnectionDoubleLock connection = DbConnectionDoubleLock.getInstance();

		System.out.println(connection);
	}
}
